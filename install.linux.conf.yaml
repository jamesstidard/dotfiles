- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.config/sublime-merge/Packages/User: configs/sublime-merge/Packages/User

- brewfile:
    file: configs/brew/Brewfile
    stdout: true
    include: ['tap', 'cask']
