- defaults:
    link:
        relink: true
        create: true

- clean: ['~']

- link:
    ~/.ssh/authorized_keys: configs/ssh/personal.authorized_keys
    ~/.gitconfig: configs/git/.personal.gitconfig
